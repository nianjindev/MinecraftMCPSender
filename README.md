# Minecraft MCP
Connects to `http://localhost:8080` and sends data such as interactions with blocks, entities, chat messages, as well as environmental changes.

### MCP Server
Run the MCP server with MinecraftNarratorMCP

Make sure do `pip install -r requirements.txt` to install all dependencies
